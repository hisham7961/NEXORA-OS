# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem
.idea
.vscode

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (keep .env for local dev DB so the foundation runs out-of-the-box;
# never commit real secrets — production uses managed secrets, see README)
.env.local
.env.production
.env*.local

# database (local sqlite dev database + prisma engine cache)
/prisma/*.db
/prisma/*.db-journal
/prisma/migrations/dev.db

# testing
/coverage

# typescript
*.tsbuildinfo
